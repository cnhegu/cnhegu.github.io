[siteBrand]
    # 保持为空或极简，让主题在无法渲染有效的 SVG 时，回退显示 site title
    # (此方法可能受主题版本影响，但值得一试)

    # 或者，如果您有自己的 SVG 代码，请放在此处：
    # body = '<svg>...</svg>'

    # 最简单的替代方案是直接使用您的 Logo 图片路径：
    body = '<img src="/icons/mylogo.png" alt="何故小站 Logo" style="height: 100%; width: auto;"/>'